# Copy this file to .env.yaml and fill in your values before deploying.
# .env.yaml is gitignored — never commit it with real credentials.
SUPABASE_URL: "https://<your-project>.supabase.co"
SUPABASE_KEY: "<your-supabase-service-role-key>"
LOG_LEVEL: "INFO"
