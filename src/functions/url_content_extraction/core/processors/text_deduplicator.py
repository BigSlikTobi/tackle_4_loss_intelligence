"""Placeholder for paragraph deduplication utilities."""

from ..contracts.extracted_content import ExtractedContent


def deduplicate_paragraphs(content: ExtractedContent) -> ExtractedContent:
    """Remove duplicate paragraphs while preserving order (implementation pending)."""
    return content
