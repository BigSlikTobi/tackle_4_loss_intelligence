# Optional environment variables for local testing
# The API allows overriding all keys via request payload. These values are only used
# when payload fields are absent.
GOOGLE_CUSTOM_SEARCH_KEY=
GOOGLE_CUSTOM_SEARCH_ENGINE_ID=
SUPABASE_URL=
SUPABASE_KEY=
