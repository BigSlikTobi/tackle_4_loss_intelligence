# Article validation module configuration template
#
# Request-scoped credentials are preferred. These values are only used as
# fallbacks when per-request credentials are not provided.

# Optional: override default log verbosity for the module
LOG_LEVEL=INFO

# Optional: fallback OpenAI credentials (use request payload whenever possible)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-nano

# Optional: fallback Supabase connection (request payload takes precedence)
SUPABASE_URL=
SUPABASE_KEY=
SUPABASE_TABLE=article_validations

# Optional: adjust default validation behaviour
VALIDATION_TIMEOUT_SECONDS=90
