# Article Validation Module Configuration
#
# NOTE: Request-scoped credentials are PREFERRED and RECOMMENDED.
# Provide credentials in each request payload rather than using environment variables.
# These environment variables are only used as fallbacks when request payload
# credentials are not provided.
#
# See README.md for complete configuration documentation.

# ============================================================================
# Logging Configuration
# ============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================================================
# LLM Configuration (Gemini API - Fallback)
# ============================================================================
# RECOMMENDED: Provide these in request payload via "llm" block
# Only set here if you want fallback credentials for all requests

# Google Gemini API key (required for validation)
GEMINI_API_KEY=your-gemini-api-key-here
# Alternative environment variable names also supported:
# GOOGLE_API_KEY=your-google-api-key-here
# OPENAI_API_KEY=your-openai-api-key-here

# Gemini model to use for validation
GEMINI_MODEL=gemini-2.5-flash-lite
# Alternative: GOOGLE_MODEL=gemini-2.5-flash-lite
# Alternative: OPENAI_MODEL=gpt-4o-mini

# Enable web search for fact verification (true/false)
GEMINI_ENABLE_WEB_SEARCH=true
# Alternative: GOOGLE_ENABLE_WEB_SEARCH=true
# Alternative: OPENAI_ENABLE_WEB_SEARCH=true

# Timeout for individual LLM API calls (seconds)
GEMINI_TIMEOUT_SECONDS=60
# Alternative: GOOGLE_TIMEOUT_SECONDS=60
# Alternative: OPENAI_TIMEOUT_SECONDS=60

# ============================================================================
# Validation Configuration
# ============================================================================

# Total validation timeout (seconds)
# If validation takes longer, partial results are returned
VALIDATION_TIMEOUT_SECONDS=90

# Validation thresholds (0.0 to 1.0)
# Articles below these thresholds may be rejected
# VALIDATION_FACTUAL_THRESHOLD=0.7
# VALIDATION_CONTEXTUAL_THRESHOLD=0.7
# VALIDATION_QUALITY_THRESHOLD=0.7
# VALIDATION_CONFIDENCE_THRESHOLD=0.8

# ============================================================================
# Supabase Configuration (Optional - Fallback)
# ============================================================================
# RECOMMENDED: Provide these in request payload via "supabase" block
# Only set here if you want all validation reports stored automatically

# Supabase project URL
SUPABASE_URL=https://your-project.supabase.co

# Supabase service role key (or anon key with appropriate RLS policies)
SUPABASE_KEY=your-supabase-service-role-key

# Table name for storing validation reports
SUPABASE_TABLE=article_validations

# Schema name (optional, defaults to public)
# SUPABASE_SCHEMA=public

# ============================================================================
# Usage Examples
# ============================================================================
#
# REQUEST-SCOPED CREDENTIALS (Recommended):
# {
#   "article": {...},
#   "article_type": "team_article",
#   "llm": {
#     "api_key": "your-gemini-api-key",
#     "model": "gemini-2.5-flash-lite",
#     "enable_web_search": true
#   },
#   "supabase": {
#     "url": "https://your-project.supabase.co",
#     "key": "your-supabase-key"
#   }
# }
#
# ENVIRONMENT VARIABLE FALLBACK:
# Set the variables above and omit "llm" and "supabase" blocks from requests
#
# ============================================================================
